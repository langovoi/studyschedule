{% extends '/views/layouts/main.twig' %}

{% block title %}Преподаватели{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                {% include '/views/particle/notice.twig' %}
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <a href="{{ this.createUrl('create') }}" class="btn btn-default pull-right"><span
                            class="glyphicon glyphicon-plus"></span></a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                {% if teachers|length %}
                    <table class="table table-hover">
                        <thead>
                        <tr>
                            {% set labels = model.attributeLabels() %}
                            {% for attribute,value in model.attributes %}
                                <th>{{ labels[attribute] }}</th>
                            {% endfor %}
                            <th>Действия</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for teacher in teachers %}
                            <tr>
                                {% for attribute,value in teacher.attributes %}
                                    {% if attribute != 'owner_id' %}
                                        <td>{{ value }}</td>
                                    {% else %}
                                        <td>{% if value %}{{ teacher.owner.username }}{% else %}-Нет-{% endif %}</td>
                                    {% endif %}
                                {% endfor %}
                                <td>
                                    {% if App.user.checkAccess('admin') or teacher['owner_id'] and App.user.id == teacher.owner_id %}
                                        <a href="{{ this.createUrl('update', {id: teacher.id}) }}"
                                           class="btn btn-sm btn-default"><span
                                                    class="glyphicon glyphicon-pencil"></span></a>
                                        <a href="{{ this.createUrl('delete', {id: teacher.id}) }}"
                                           class="btn btn-sm btn-danger"><span class="glyphicon glyphicon-trash"></span></a>
                                    {% else %}
                                        Нет доступа
                                    {% endif %}
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    <div class="alert alert-warning">Нет преподавателей</div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}