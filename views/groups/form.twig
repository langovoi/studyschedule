{% extends '/views/layouts/main.twig' %}
{% if model.isNewRecord %}
    {% set title = 'Добавление группы' %}
{% else %}
    {% set title = 'Редактирование группы ' ~ model.number %}
{% endif %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6">
                {% include '/views/particle/notice.twig' %}
                {% set form = this.beginWidget('CActiveForm',
                {
                'errorMessageCssClass': 'alert alert-danger'
                }) %}
                <div class="form-group text-center">
                    <h1>{{ title }}</h1>
                </div>
                {% include '/views/particle/notice.twig' %}
                <div class="form-group">
                    {{ form.label(model, 'number') }}
                    {{ form.textField(model, 'number', {'class': 'form-control', 'placeholder': model.attributeLabels().number }) }}
                </div>
                {{ form.error(model, 'number') }}
                <div class="form-group">
                    {{ form.label(model, 'owner_id') }}
                    {{ form.dropDownList(model, 'owner_id', users_list, {'class': 'form-control', 'placeholder': model.attributeLabels().owner_id }) }}
                </div>
                {{ form.error(model, 'owner_id') }}
                <div class="form-group text-center">
                    {{ C.Html.submitButton(model.isNewRecord ? 'Создать' : 'Сохранить', {'class': 'btn btn-success'}) }}
                </div>
                {{ void(this.endWidget()) }}
            </div>
        </div>
    </div>
{% endblock %}