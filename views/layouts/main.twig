<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <title>{% block title %}Учебное расписание ККЭП{% endblock %}</title>
    {{ void(App.clientScript.registerCoreScript('jquery')) }}
    {{ void(App.clientScript.registerScriptFile('/js/app.js')) }}
    {{ void(App.clientScript.registerCssFile('/css/app.css')) }}
</head>
<body>
{% include 'views/particle/menu.twig' %}
{% block content %}{% endblock %}
<footer><a href="{{ App.createUrl('feedback') }}">Обратная связь</a> | <a href="http://vk.com/marklangovoi" target="_blank">Марк Ланговой</a> &copy; {% if "now"|date('Y') != 2014 %}2014-{{ "now"|date('Y') }}{% else %}{{ "now"|date('Y') }}{% endif %}</footer>
{% if constant('YII_DEBUG') == 0 %}
    <!-- Yandex.Metrika counter -->
    <script type="text/javascript">
        (function (d, w, c) {
            (w[c] = w[c] || []).push(function () {
                try {
                    w.yaCounter26625756 = new Ya.Metrika({
                        id: 26625756,
                        webvisor: true,
                        clickmap: true,
                        trackLinks: true,
                        accurateTrackBounce: true,
                        ut: "noindex"
                    });
                } catch (e) {
                }
            });

            var n = d.getElementsByTagName("script")[0],
                    s = d.createElement("script"),
                    f = function () {
                        n.parentNode.insertBefore(s, n);
                    };
            s.type = "text/javascript";
            s.async = true;
            s.src = (d.location.protocol == "https:" ? "https:" : "http:") + "//mc.yandex.ru/metrika/watch.js";

            if (w.opera == "[object Opera]") {
                d.addEventListener("DOMContentLoaded", f, false);
            } else {
                f();
            }
        })(document, window, "yandex_metrika_callbacks");
    </script>
    <noscript>
        <div><img src="//mc.yandex.ru/watch/26625756?ut=noindex" style="position:absolute; left:-9999px;" alt=""/></div>
    </noscript>
    <!-- /Yandex.Metrika counter -->
    <script>
        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                (i[r].q = i[r].q || []).push(arguments)
            }, i[r].l = 1 * new Date();
            a = s.createElement(o),
                    m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

        ga('create', 'UA-47137792-2', 'auto');
        ga('send', 'pageview');

    </script>
{% endif %}
</body>
</html>